---
interface Props {
  slug: string
  number: number
  name: string | undefined
}

const { slug, number, name } = Astro.props
---

<a
  href={slug}
  class="text-slate-700 text-sm transition-colors duration-150 hover:text-slate-300"
>
  Art. {number}
  {name}
</a>
