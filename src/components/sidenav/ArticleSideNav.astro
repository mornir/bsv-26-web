---
interface Props {
  slug: string
  number: number
  name: string | undefined
}

const { slug, number, name } = Astro.props

const pathname = Astro.url.pathname
---

<a
  href={slug}
  class:list={[
    'text-slate-700 text-sm transition-colors duration-150 hover:text-slate-300',
    { 'font-bold': pathname === slug },
  ]}
>
  Art. {number}
  {name}
</a>
