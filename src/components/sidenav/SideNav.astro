---
import { getIndex } from '@/sanity/queries'
import { buildToc } from '@/utils/helpers'
import { formatHeading } from '@/i18n/utils'
import ArticleSideNav from '@/components/sidenav/ArticleSideNav.astro'
import TitleSideNav from '@/components/sidenav/TitleSideNav.astro'
import { getLocale, getLocaleUrl } from 'astro-i18n-aut'
const locale = getLocale(Astro.url) as LangKeys
const index = await getIndex()

const toc = buildToc(index)

function artURL(slug: string) {
  return getLocaleUrl('/art/' + slug, locale)
}
---

<aside class="sticky top-0">
  <nav class="space-y-4 p-6 bg-slate-100 h-screen overflow-y-scroll">
    {
      toc.map((title) => (
        <div>
          <TitleSideNav
            name={`${formatHeading({ number: title.number, type: 'title' }, locale)}: ${title.name[locale]}`}
          >
            <div class="ml-2 space-y-6">
              {/* Articles directly under title */}
              {title.articles.length > 0 && (
                <ul class="space-y-1">
                  {title.articles.map((article) => (
                    <li>
                      <ArticleSideNav
                        slug={artURL(article.slug)}
                        number={article.number}
                        name={article.name[locale]}
                      />
                    </li>
                  ))}
                </ul>
              )}

              {/* Sections directly under title */}
              {title.sections.map((section) => (
                <div>
                  <h3 class="mb-1 text-slate-600 text-sm font-semibold">
                    {formatHeading(
                      { number: section.number, type: 'section' },
                      locale
                    )}{' '}
                    {section.name[locale]}
                  </h3>

                  <ul class="mt-1 space-y-1 pl-2">
                    {section.articles.map((article) => (
                      <li>
                        <ArticleSideNav
                          slug={artURL(article.slug)}
                          number={article.number}
                          name={article.name[locale]}
                        />
                      </li>
                    ))}
                  </ul>
                </div>
              ))}

              {/* Chapters */}
              {title.chapters.map((chapter) => (
                <div class="space-y-3 pl-2 mt-2">
                  <h3 class="mb-1 font-semibold text-slate-600  text-sm">
                    {formatHeading(
                      { number: chapter.number, type: 'chapter' },
                      locale
                    )}
                    — {chapter.name[locale]}
                  </h3>

                  {/* Articles directly under chapter */}
                  {chapter.articles.length > 0 && (
                    <ul class="space-y-1 pl-2">
                      {chapter.articles.map((article) => (
                        <li>
                          <ArticleSideNav
                            slug={artURL(article.slug)}
                            number={article.number}
                            name={article.name[locale]}
                          />
                        </li>
                      ))}
                    </ul>
                  )}

                  {/* Sections inside chapter */}
                  {chapter.sections.map((section) => (
                    <div class="pl-6">
                      <h4 class="mb-1 text-slate-700">
                        {formatHeading(
                          { number: section.number, type: 'section' },
                          locale
                        )}
                        — {section.name[locale]}
                      </h4>

                      <ul class="mt-1 space-y-1 pl-2">
                        {section.articles.map((article) => (
                          <li>
                            <ArticleSideNav
                              slug={artURL(article.slug)}
                              number={article.number}
                              name={article.name[locale]}
                            />
                          </li>
                        ))}
                      </ul>
                    </div>
                  ))}
                </div>
              ))}
            </div>
          </TitleSideNav>
        </div>
      ))
    }
  </nav>
</aside>
