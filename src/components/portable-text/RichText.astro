---
import { PortableText } from 'astro-portabletext'
import InternalLink from './InternalLink.astro'
import Latex from './Latex.astro'
import FigureModal from './FigureModal.astro'
import Superscript from './Superscript.astro'
import Subscript from './Subscript.astro'
import List from './List.astro'
import Block from './Block.astro'
import TableModal from './TableModal.astro'

interface Props {
  blocks: any
}

const { blocks } = Astro.props

const components = {
  list: List,
  block: Block,
  mark: {
    internalLink: InternalLink,
    figure: FigureModal,
    sup: Superscript,
    sub: Subscript,
  },
  type: {
    latex: Latex,
    table: TableModal,
  },
}
---

<PortableText value={blocks} components={components} />
