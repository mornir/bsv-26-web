---
import Layout from '@/layouts/Layout.astro'
import TocNav from '@/components/toc/TocNav.astro'

interface Props {
  title: string
  tabTitel?: string
}

const { title, tabTitel = title } = Astro.props
---

<Layout title={tabTitel}>
  <div class="mt-8 grid grid-cols-[20rem_1fr_20rem] gap-x-4 px-4">
    <TocNav />
    <main class="max-w-3xl justify-self-center" data-pagefind-body>
      <slot name="context" />
      <h1 class="text-center font-semibold font-serif text-3xl mt-8 mb-6">
        {title}
      </h1>
      <slot />
    </main>
    <slot name="on-this-page" />
  </div>
</Layout>
